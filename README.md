# LNS - Local Network Scanner

![Bash](https://img.shields.io/badge/Bash-Script-green.svg)
![License](https://img.shields.io/badge/License-MIT-blue.svg)
![Platform](https://img.shields.io/badge/Platform-Linux-lightgrey.svg)

LNS (Local Network Scanner) - это простой, но мощный bash-скрипт для сканирования локальной сети и обнаружения активных устройств с помощью ARP-запросов.

## Особенности

- **Простота использования** - одна команда для сканирования сетей
- **Поддержка множества сетей** - сканируйте несколько подсетей одновременно
- **Двойной вывод** - результаты в JSON и TXT форматах
- **Автоматическое определение интерфейсов** - работает с любыми сетевыми интерфейсами
- **Чистый вывод** - фильтрация служебных сообщений arp-scan
- **Кросс-платформенность** - работает на любом дистрибутиве Linux

## Установка

### Предварительно
```bash
# Установите необходимые пакеты (Debian/Ubuntu)
sudo apt update
sudo apt upgrade
sudo apt install arp-scan jq libpcap-devel
```
```bash
# Для CentOS/RHEL
sudo yum install arp-scan jq libpcap-devel
```
```bash
# Для новых версий Fedora:
sudo dnf install arp-scan jq libpcap-devel
```

#### Также можно arp-scan установить по [ссылке](https://github.com/royhills/arp-scan.git) в случае ошибки "Невозможно найти пакет arp-scan" или запустить скриптом
### Установка скрипта
```bash
git clone https://github.com/Juteex2/LNS.git
cd LNS
chmod +x arpscan.sh
# Скрипт автоматической установки arp-scan
sudo ./arpscan.sh
chmod +x scanlist.sh
```
## 🛠 Использование
Базовое использование
bash
sudo ./scanlist.sh <сеть1> [сеть2] [сеть3] ...
Примеры
Сканирование одной сети:

```bash
sudo ./scanlist.sh 192.168.1.0/24
```
Сканирование нескольких сетей:

```bash
sudo ./scanlist.sh 192.168.1.0/24 192.168.2.0/24 10.0.0.0/24
```
Сканирование с разными масками:

```bash
sudo ./scanlist.sh 192.168.1.0/24 172.16.0.0/16 10.0.0.0/28
```
### 📊 Выходные данные
Скрипт создает два файла в директории /var/www/:

JSON формат (list.json)
```json
{
  "scan_info": {
    "timestamp": "2025-11-22T10:30:00+07:00",
    "total_devices": 4,
    "target_networks": ["192.168.1.0/24"]
  },
  "devices": [
    {
      "IP": "192.168.1.1",
      "MAC": "ff:ff:ff:ff:ff:ff",
      "info": "Router Manufacturer"
    }
  ]
}
```
## Текстовый формат (list.txt)
```text
# Сканирование от Sat Nov 22 10:30:00 +07 2025
# Сканируемые сети: 192.168.1.0/24
# IP-адрес MAC-адрес Производитель
==============================================
192.168.1.1     aa:bb:cc:dd:ee:ff       Router Manufacturer
192.168.1.100   11:22:33:44:55:66       Computer Manufacturer
192.168.1.101   22:33:44:55:66:77       Phone Manufacturer
```
### Применение
#### Сетевой мониторинг
- Регулярное сканирование для обнаружения новых устройств
- Мониторинг доступности устройств в сети
#### Инвентаризация сети
- Автоматическое создание списка сетевых устройств
- Отслеживание MAC-адресов и производителей
#### Интеграция с другими системами
- JSON формат для легкой интеграции с веб-приложениями
- Автоматизация с помощью cron задач

## Пример cron задачи для ежедневного сканирования
```bash
# Добавьте в crontab (crontab -e)
0 2 * * * /path/to/LNS/scanlist.sh 192.168.1.0/24
```
## :white_check_mark: Детали реализации
- Поддерживаемые интерфейсы
- Скрипт автоматически определяет и пробует следующие интерфейсы:
  + wlan0, wlan1 (Wi-Fi интерфейсы)
  + eth0, eth1 (Ethernet интерфейсы)
  + Другие активные интерфейсы

## Алгоритм работы
- Проверка корректности указанных сетей
- Автоматическое определение активных сетевых интерфейсов
- Последовательное сканирование каждой сети через доступные интерфейсы
- Фильтрация и обработка результатов
- Сохранение в JSON и TXT форматах
## Устранение неполадок
Ошибка: "arp-scan не установлен"
```bash
sudo apt install arp-scan
```
Ошибка: "jq не установлен"
```bash
sudo apt install jq
```
Ошибка: "Отказано в доступе" или "Операция не позволена"
```bash
Убедитесь, что запускаете с правами root
sudo ./scanlist.sh 192.168.1.0/24
```
## ⭐ Лицензия
Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.
